services:
  osrm:
    image: ghcr.io/project-osrm/osrm-backend:latest
    container_name: intercity-osrm
    ports:
      - "5000:5000"
    volumes:
      - ./data/osrm:/data
    command: osrm-routed --algorithm mld /data/uzbekistan-latest.osrm
